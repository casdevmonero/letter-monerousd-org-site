# Letter — letter.monerousd.org

MoneroUSD sovereign mirror — letter.monerousd.org. 100% private P2P messaging:
post-quantum sealed end to end, zero metadata, USDm payments in-chat. No email,
no phone, no IP — your wallet is your identity.

Static frontend only: the BACKEND IS THE CHAIN. Every seal/open/relay/pay runs in
the user's own wallet + node via the provider bridge; this mirror exists for
unauthenticated browser access. Default-deny gitignore prevents secret leaks.
